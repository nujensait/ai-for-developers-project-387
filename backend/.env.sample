# Default environment variables (override locally with .env.local)
APP_ENV=dev
APP_DEBUG=1
APP_SECRET=2f9c1b7a4e8d3056c1a9f4b2e7d80c35

# HTTP port for the built-in PHP server
PORT=8081

# SQLite storage (data persists between restarts)
DATABASE_URL="sqlite:///%kernel.project_dir%/var/data.db"

# Availability / working-hours configuration
APP_WORKING_HOURS_START=9
APP_WORKING_HOURS_END=21
APP_AVAILABILITY_DAYS=14
